#include <stdio.h>
#include <string.h>

int main() {
    char s[1000];
    
    // Read full sentence including spaces
    fgets(s, sizeof(s), stdin);

    // Split using space delimiter
    char *token = strtok(s, " ");
    while (token != NULL) {
        printf("%s\n", token);
        token = strtok(NULL, " ");
    }

    return 0;
}
